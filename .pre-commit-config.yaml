default_install_hook_types: [pre-commit, pre-push]

repos:
  # Formatiert den Code automatisch bei jedem "commit"
  - repo: https://github.com/psf/black
    rev: 24.4.2
    hooks:
      - id: black
        stages: [pre-commit]

  # Führt die Tests bei jedem "push" aus
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: pytest
        language: system
        types: [python]
        pass_filenames: false
        always_run: true
        stages: [pre-push]
